const tool = {
  add (...numbers) {
    const sum = numbers.reduce((count, item) => count + item);
    return sum;
  },
  multiply (...numbers) {
    const result = numbers.reduce((count, item) => count * item);
    return result;
  }
};
module.exports = tool;
